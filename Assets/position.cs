using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class position : MonoBehaviour {
    
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
    private void FixedUpdate()
    {
        //Camera.main.transform.position = new Vector3(this.transform.position.y, this.transform.position.x, Camera.main.transform.position.z);
    }
}
